# AI Research Assistant

Современное FastAPI приложение для продуктового исследования с использованием ИИ и продвинутой системой анализа данных.

## 🚀 Возможности

- **Красивый современный дизайн** с анимированным фоном и градиентами
- **Интерактивные формы** для ввода параметров исследования (фичи и продукты)
- **Интеграция с Google Gemini AI** для генерации детальных отчетов
- **Real-time WebSocket** обновления прогресса исследования
- **Проверка ссылок** с автоматическим поиском дополнительных источников
- **Экспорт в PDF** и копирование результатов
- **Адаптивный дизайн** для всех устройств
- **База данных SQLite** для хранения отчетов и сессий

## 📋 Требования

- Python 3.8+
- Google Gemini API ключ
- SQLite (встроенная)

## 🛠 Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd Maclay
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` и добавьте ваш Gemini API ключ:
```env
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_API_URL=https://generativelanguage.googleapis.com
GEMINI_MODEL=gemini-2.5-flash
```

## 🚀 Запуск

### Локальная разработка:
```bash
python main.py
```

### Продакшн с nginx:
```bash
uvicorn main:app --host 127.0.0.1 --port 8000 --proxy-headers --forwarded-allow-ips="*"
```

Приложение будет доступно по адресу: http://localhost:8000

## 📱 Использование

1. **Главная страница**: Выберите тип исследования (Фича или Продукт)
2. **Форма исследования**: Заполните параметры исследования
3. **Real-time прогресс**: Следите за этапами анализа через WebSocket
4. **Результаты**: Просматривайте детальный отчет с проверенными ссылками
5. **Экспорт**: Сохраняйте результаты в PDF или копируйте текст

## 🔄 Процесс исследования

### Этапы анализа:
1. **Сбор данных** - Поиск компаний и продуктов по критериям
2. **Анализ кейсов** - Детальный анализ найденных примеров
3. **Проверка ссылок** - Верификация и поиск дополнительных источников
4. **Генерация отчета** - Создание финального отчета с рекомендациями

### Особенности:
- **Автоматический поиск ссылок** - ИИ находит дополнительные источники при их отсутствии
- **Проверка доступности** - Все ссылки проверяются на работоспособность
- **Уникальные страны** - Подсчет географического охвата исследования
- **Real-time статистика** - Отображение прогресса в реальном времени

## 🎨 Дизайн

- **Современный градиентный фон** с анимированными элементами
- **Плавные анимации** и переходы между страницами
- **Адаптивная верстка** для мобильных устройств и десктопов
- **Интуитивно понятный интерфейс** с пошаговыми формами
- **Темная/светлая тема** с автоматическим переключением

## 🔧 API Endpoints

### Основные маршруты:
- `GET /` - Главная страница
- `GET /feature` - Форма исследования фичи
- `GET /product` - Форма исследования продукта
- `POST /process-feature` - Обработка формы фичи
- `POST /process-product` - Обработка формы продукта
- `GET /results/{report_id}` - Страница результатов
- `GET /reports` - Список всех отчетов
- `DELETE /reports/{report_id}` - Удаление отчета

### WebSocket:
- `WS /ws/{client_id}` - Real-time обновления прогресса

## 📊 Структура проекта

```
Maclay/
├── main.py                 # Основное приложение FastAPI
├── research_stages.py      # Логика этапов исследования
├── services.py            # Сервисы для работы с БД
├── database.py             # Модели базы данных
├── config.py               # Конфигурация приложения
├── requirements.txt        # Зависимости Python
├── data/
│   └── maclay_research.db # База данных SQLite
├── templates/             # HTML шаблоны
│   ├── index.html         # Главная страница
│   ├── feature_form.html  # Форма исследования фичи
│   ├── product_form.html  # Форма исследования продукта
│   ├── loading.html       # Страница загрузки с WebSocket
│   └── results.html       # Страница результатов
├── static/               # Статические файлы
│   ├── css/
│   │   └── style.css     # Стили CSS
│   ├── js/
│   │   └── main.js       # JavaScript
│   └── logo.jpg          # Логотип приложения
└── README.md             # Документация
```

## 🎯 Особенности

### ИИ и анализ:
- **Google Gemini Integration** - Использует продвинутые промпты для генерации отчетов
- **Автоматический поиск источников** - ИИ находит дополнительные ссылки при необходимости
- **Проверка ссылок** - Автоматическая верификация всех найденных источников
- **Умная структуризация** - Автоматическое форматирование отчетов в Markdown

### Технические особенности:
- **WebSocket соединения** - Real-time обновления без перезагрузки страницы
- **Retry механизм** - Автоматические повторы при ошибках API
- **Очистка соединений** - Автоматическое управление WebSocket соединениями
- **Кросс-окружение** - Работает на HTTP и HTTPS с автоматическим выбором протокола

### UI/UX:
- **Responsive Design** - Адаптивная верстка для всех устройств
- **Modern UI/UX** - Красивый дизайн с анимациями и градиентами
- **PDF Export** - Возможность экспорта результатов в PDF
- **Real-time Loading** - Анимированная страница загрузки с прогрессом
- **Интерактивные формы** - Пошаговое заполнение с валидацией

## 🔑 Получение Gemini API ключа

1. Зарегистрируйтесь на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Создайте новый API ключ
3. Добавьте ключ в файл `.env`:
```env
GEMINI_API_KEY=your-gemini-api-key-here
```

## 🚀 Деплой на сервере

### Nginx конфигурация для WebSocket:
```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        
        # WebSocket поддержка
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты для WebSocket
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
        proxy_connect_timeout 86400;
        
        # Буферизация для WebSocket
        proxy_buffering off;
        proxy_cache off;
    }
}
```

### Systemd сервис:
```ini
[Unit]
Description=AI Research Assistant
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/Maclay
ExecStart=/usr/bin/python3 main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

## 📝 Лицензия

MIT License

## 🤝 Вклад в проект

Приветствуются любые улучшения и предложения! Особенно полезны:
- Новые типы исследований
- Улучшения UI/UX
- Оптимизация производительности
- Дополнительные интеграции с ИИ

## 📞 Поддержка

Если у вас есть вопросы или проблемы:
1. Создайте issue в репозитории
2. Проверьте логи приложения
3. Убедитесь в правильности API ключей

## 🔄 Changelog

### v2.0.0
- ✅ Интеграция с Google Gemini AI
- ✅ Real-time WebSocket обновления
- ✅ Автоматическая проверка ссылок
- ✅ База данных для хранения отчетов
- ✅ Улучшенный UI/UX дизайн
- ✅ Поддержка HTTPS с WSS протоколом
- ✅ Retry механизм для надежности
- ✅ Адаптивная верстка для мобильных устройств